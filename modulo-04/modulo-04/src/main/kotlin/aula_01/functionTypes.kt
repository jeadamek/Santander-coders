package aula_01

// Calcula Descontos
// passar funcao para dentro de uma variavel
val calculaDesconto: (Double, Double) -> Double = { valor, desconto ->
    (desconto * valor) / 100
}

fun main() {

}